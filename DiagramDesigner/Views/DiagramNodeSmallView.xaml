<UserControl x:Class="DiagramDesigner.Views.DiagramNodeSmallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Border BorderThickness="0" CornerRadius="6" MinWidth="30"  Background="SlateBlue">

        <TextBox Name="Name" Margin="7,2" Foreground="White" Text="3f231" FontFamily="Verdana" 
                 Background="SlateBlue" IsHitTestVisible="{Binding CanEditName}" FontSize="10" TextAlignment="Center" BorderThickness="0" BorderBrush="Transparent"
                 FontWeight="Bold" VerticalContentAlignment="Center" AcceptsReturn="True"></TextBox>
    </Border>
</UserControl>
