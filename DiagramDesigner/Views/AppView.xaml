<Window x:Class="DiagramDesigner.AppView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sample diagram designer" Height="500" Width="725" WindowStartupLocation="CenterScreen">
    <DockPanel LastChildFill="True" >
        <WrapPanel Margin="0,0,0,0" DockPanel.Dock="Top">
            <ListBox Name="commands" ItemsSource="{Binding Diagram1.Commands}" SelectedItem="{Binding Diagram1.SelectedCommand}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"></StackPanel>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.BorderThickness>0</ListBox.BorderThickness>
            </ListBox>
            <Button Name="SaveDiagram" Margin="3">Save diagram</Button>
            <Button Name="LoadDiagram" Margin="3">Load diagram</Button>
            <Button Name="ClearDiagram" Margin="3">Clear</Button>
        </WrapPanel>
       <ContentControl DockPanel.Dock="Bottom" Name="Diagram1"></ContentControl>
    </DockPanel>
</Window>
